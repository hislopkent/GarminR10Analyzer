services:
  - type: web
    name: GarminR10Analyzer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.enableCORS false
    plan: free
    envVars:
      - key: PASSWORD
        sync: false
